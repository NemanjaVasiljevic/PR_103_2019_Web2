namespace PR_103_2019.Models
{
    public enum Role
    {
        ADMIN,
        USER,
        SELLER
    }
    public enum ArticleState
    {
        AVAILABLE,
        RESERVED,
        SHIPPING,
        ARRIVED,
        RETURNED
    }
}
